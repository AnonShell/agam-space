# Docs

Documentation site for Agam Space built with Docusaurus.

## Installation

```bash
cd docs
npm install
```

## Development

```bash
npm start
```

This command starts a local development server at `http://localhost:3000`.

## Build

```bash
npm run build
```

This generates static content into the `build` directory.
